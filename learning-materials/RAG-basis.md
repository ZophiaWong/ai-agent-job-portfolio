高频且高 ROI 的点，基本集中在 6 组：
(1) 文档处理、chunking、metadata/filtering；
(2) embeddings、vector DB、dense vs hybrid retrieval；
(3) grounding、citation、拒答策略、basic guardrails；
(4) eval、benchmark、failure analysis、observability；
(5) latency / cost / caching / freshness；
(6) RAG 在 agent/workflow 中的角色，以及它和 fine-tuning / tools / long-context 的边界。
